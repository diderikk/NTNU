package idatt2106.group3.backend.Enum;

/**
 * Enumerator used as default for sorting activities.
 * User can choose to sort by DATE, DISTANCE or PARTICIPANT_AMOUNT.
 */
public enum SortingType {
    DATE, DISTANCE, PARTICIPANT_AMOUNT, NONE
}
